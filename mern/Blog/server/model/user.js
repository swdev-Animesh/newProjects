import mongoose from "mongoose";

const UserSchema = {
  name: {
    type: String,
    required: true,
  },
  email: {
    type: String,
    required: true,
    unique: true,
  },
  password: {
    type: String,
    required: true,
  },
};

mongoose.Model("user", UserSchema);

export default UserSchema;
